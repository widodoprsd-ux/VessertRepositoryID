Place your video binaries here.

Supported extensions: mp4, webm, ogg

The build script copies every file in this folder into
dist/assets/source/video/ and adds a content hash.

Video encoding is not implemented in pure Node. Supply
pre-encoded files and commit them.