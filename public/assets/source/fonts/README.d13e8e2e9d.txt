Place your font binaries here.

Supported extensions: woff2, woff, ttf, otf, eot

The build script copies every file in this folder into
dist/assets/source/fonts/ and adds a content hash.

Do NOT download fonts from a CDN at build time.
Bring your own licensed font files and commit them.